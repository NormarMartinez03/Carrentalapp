
  # Car Rental App

  This is a code bundle for Car Rental App. The original project is available at https://www.figma.com/design/h2d2wO51giaNg7DAU5P0XI/Car-Rental-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  